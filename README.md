# DECO3500_Music
Music Sharing

Next steps
Backend
Add helper functions to get url of spotify profile picture given a user_id,
Add helper functions to get url of track image album cover given a user_id
Change update to update the image field and poll logic to provide a image url field

Frontend
Display image url field in the centre of each pin for each user


Other
Logic to join users music
Logic to change colour of pin based on how many users are joined, so table of "jams" with list of users listening checked against the polled/updated data
Maybe animation for big jams etc

Increase the rate of updating data maybe reduce the rounding on the coords so less jumping

